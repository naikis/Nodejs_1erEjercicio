<div class="container-fluid bg-dark" style= "height : 85vh">
    <div class="row justify-content-center align-items-center" style="height : 76vh">
        <div class="col-auto mt-5">
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title">{{usuario.username}}</h5>
                    <p class="card-text">mail: {{usuario.mail}}</p>
                    <p class="card-text">telefono: {{usuario.telefono}}</p>
                    <p class="card-text">fecha de creacion: {{usuario.ts_create}}</p>
                    <a href="/usuarios/edit/{{id}}" class="btn btn-info">Editar</a>
                </div>
            </div>
        </div>
    </div>
</div>